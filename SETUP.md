# VRYNT Setup Instructions

## Quick Start

1. **Install Dependencies**
   ```bash
   npm install
   ```

2. **Start Development Server**
   ```bash
   npm start
   ```
   App will open at http://localhost:3000

3. **Build for Production**
   ```bash
   npm run build
   ```

4. **Deploy to Netlify**
   - Option A: Drag and drop the `build` folder to Netlify
   - Option B: Connect your Git repository to Netlify
   - Option C: Use Netlify CLI:
     ```bash
     npm install -g netlify-cli
     netlify deploy --prod
     ```

## Features

- Home page with hero section
- Shop/Collection page with product filtering
- How It Works/Playbook page
- Shopping cart
- About, FAQ, Contact, Legal pages
- Fully responsive design
- Brutalist scrapbook theme

All pages are working and ready to deploy!
